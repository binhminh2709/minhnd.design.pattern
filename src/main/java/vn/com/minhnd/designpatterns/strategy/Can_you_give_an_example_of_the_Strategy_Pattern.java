package vn.com.minhnd.designpatterns.strategy;

public class Can_you_give_an_example_of_the_Strategy_Pattern {
/**
 * The Strategy Pattern enables you to easily swap specific implementation details of an algorithm without requiring
 * a complete rewrite.
 * You can even (Tech- thuộc số chẵn; đều; bằng phẳng) swap ( danh từ - (từ lóng) sự trao đổi, sự đổi chác) implementations
 * at run time.
 * The Strategy Pattern is often used in conjunction (phép hợp) with dependency injection to allow implementations to be 
 * swapped out for test-specific code, or to allow mocked (làm giả, bắt chiếc) implementations to be used instead.
 * */
    
/**
 * The Strategy pattern có thể you de dàng đổi dứt khoát thực thi chi tiết của một thuật toán, không cùng yêu cầu
 * hoàn thành trước khi viết, ban có thể
 * */
}
